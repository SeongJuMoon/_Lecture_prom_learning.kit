kind: PodMonitor
metadata:
  labels:
    release: prometheus-stack
  namespace: monitoring
  name: mysql
spec:
  podMetricsEndpoints:
  -  port: metrics
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: mysql